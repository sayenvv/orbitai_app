# Install from Orbit_API directory:
#   python -m pip install -r requirements-local.txt
#
# This installs orbit-api plus path deps (clovai, clovai-apps, orbit-orchestration).
# Do NOT also run `pip install -e packages/orbit_orchestration` in the same command —
# pip treats that as a duplicate requirement and fails with ResolutionImpossible.
#
# After pulling orchestration changes, re-run the command above.
-e .[dev]
